apiVersion: v1
kind: Service
metadata:
  name: serviceb
  namespace: develop
  labels:
    app: serviceb
spec:
  selector:
    app: serviceb
  ports:
    - name: http
      port: 80
      targetPort: 3000
